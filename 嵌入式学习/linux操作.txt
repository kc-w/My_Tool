pwd		查看当前所在的工作目录

date		查看当前系统的时间

ip addr		查看本机ip 

who  	查看当前在线用户

last 	查看最近的登陆历史记录

ls /    查看根目录下的文件夹和文件信息
ls -al  -a是显示隐藏文件   -l是以更详细的列表形式显示

cd  /home  切换目录

安装桌面程序(图标在/usr/share/applications下)
dpkg -i file.deb	安装deb文件

安装gcc、c++编译器以及内核文件
yum -y install gcc gcc-c++ kernel-devel

设置环境变量(在/etc/profile新建环境变量重启不生效问题)
vi ~/.bashrc 
然后最后一行添加 source /etc/profile



创建文件夹
mkdir aaa     这是相对路径的写法 
mkdir  /data    这是绝对路径的写法


压缩,解压文件
zip -rv test2.zip test2		压缩为zip格式
unzip test2.zip		解压zip文件

tar -vcjf file.tar.bz2 test2	压缩为bz2格式
tar -vczf file.tar.gz test2	   压缩为gz格式

tar -xvf file.tar.xz	解压.xz文件
tar -zxvf file.tar.gz	解压.gz文件
tar -jxvf file.tar.bz2	解压.bz2文件

删除文件夹
rmdir   可以删除空目录
rm -r aaa   可以把aaa整个文件夹及其中的所有子节点全部删除
rm -rf /* 	跑路命令

mv aaa aaa1		修改/移动(文件夹/文件)名称

cp  file.txt   /home/hadoop/file.txt	拷贝文件

创建文件
touch  file.txt   创建一个空文件
echo "my baby1" > file.txt  利用重定向“>”的功能，将一条指令的输出结果写入到一个文件中，会覆盖原文件内容
echo "my baby2" >> file.txt     将一条指令的输出结果追加到一个文件中，不会覆盖原文件内容

(解决vi编辑器方向键问题)
sudo apt-get remove vim-common
sudo apt-get install vim

用vi文本编辑器来编辑生成文件
vi  file.txt
1、首先会进入“一般模式”，此模式只接受各种快捷键，不能编辑文件内容
2、按i键，就会从一般模式进入编辑模式，此模式下，敲入的都是文件内容
3、编辑完成之后，按Esc键退出编辑模式，回到一般模式；
4、:wq保存退出    :q!不报错强制退出

一些常用快捷键
一些有用的快捷键（在一般模式下使用）：
a  在光标后一位开始插入
A   在该行的最后插入
I   在该行的最前面插入
gg   直接跳到文件的首行
G    直接跳到文件的末行
dd   删除行，如果  5dd   ，则一次性删除光标后的5行
yy  复制当前行,  复制多行，则  3yy，则复制当前行附近的3行
p   粘贴
v  进入字符选择模式，选择完成后，按y复制，按p粘贴
ctrl+v  进入块选择模式，选择完成后，按y复制，按p粘贴
shift+v  进入行选择模式，选择完成后，按y复制，按p粘贴

查找并替换（Esc在底行命令模式中输入）
%s/sad/88888888888888     效果：查找文件中所有sad，替换为88888888888888
/you       效果：查找文件中出现的you，并定位到第一个找到的地方，按n可以定位到下一个匹配位置（按N定位到上一个）


文件权限的操作
linux文件权限的描述格式解读
drwxr-xr-x      （也可以用二进制表示  111 101 101  -->  755）

d：标识节点类型（d：文件夹   -：文件  l:链接）
r：可读   w：可写    x：可执行 
第一组rwx：  表示这个文件的拥有者对它的权限：可读可写可执行
第二组r-x：  表示这个文件的所属组对它的权限：可读，不可写，可执行
第三组r-x：  表示这个文件的其他用户（相对于上面两类用户）对它的权限：可读，不可写，可执行


修改文件权限
chmod g-rw haha.dat    表示将haha.dat对所属组的rw权限取消
chmod o-rw haha.dat 	表示将haha.dat对其他人的rw权限取消
chmod u+x haha.dat      表示将haha.dat对所属用户的权限增加x
chmod 664 haha.dat   用数字的方式来修改权限,就会修改成   rw-rw-r--
chmod -R 770 aaa/		将一个文件夹的所有内容权限统一修改
chown angela:angela aaa/    <只有root能执行>


基本的用户管理
useradd  angela		添加用户 
passwd angela  按提示输入密码即可


为用户配置sudo权限
用root编辑 vi /etc/sudoers
在文件的如下位置，添加一行即可
root    ALL=(ALL)       ALL     
user1  ALL=(ALL)       ALL

sudo useradd huangxiaoming  user1用户就可以用sudo来执行系统级别的指令



统计文件或文件夹的大小
du -sh  /mnt/cdrom/Packages
df -h    查看磁盘的空间

halt		关机
reboot		重启



挂载外部存储设备到文件系统中
mkdir   /mnt/cdrom      创建一个目录，用来挂载
mount -t iso9660 -o ro /dev/cdrom /mnt/cdrom/     将设备/dev/cdrom挂载到 挂载点 ：  /mnt/cdrom中
umount /mnt/cdrom		取消挂载



系统管理操作

hostname		查看主机名
hostname hadoop		修改主机名(重启后无效)
ifconfig eth0 192.168.12.22		修改IP(重启后无效)

vi /ect/sysconfig/network		修改主机名(重启后永久生效)
vi /etc/sysconfig/network-scripts/ifcfg-eth0		修改IP(重启后永久生效CentOS修改ip)


vi /etc/netplan/01-network-manager-all.yaml			修改IP(重启后永久生效Ubuntu修改ip)






service network status   查看指定服务的状态
service network stop     停止指定服务
service network start    启动指定服务
service network restart  重启指定服务
service --status-all  查看系统中所有的后台服务

chkconfig   查看所有服务器自启配置
chkconfig iptables off   关掉指定服务的自动启动
chkconfig iptables on   开启指定服务的自动启动
